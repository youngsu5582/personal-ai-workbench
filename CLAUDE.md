# personal-ai-workbench

외부 AI Provider 의 이미지 생성 작업을 비동기로 관리하는 개인 서비스.
Kotlin + Spring Boot + Spring Modulith + JPA + PostgreSQL.

학습 철학과 AI 활용 규칙은 상위 `github-project/CLAUDE.md` 를 따른다.

## 코딩 관례

### 주석은 코드가 왜 이 모양인지만 말한다

주석·KDoc 에 **그것을 알아낸 과정**을 쓰지 않는다.

```kotlin
// ❌ 사건 — 이 대화에 없던 사람은 무슨 얘긴지 모른다
/** `/v2/...` 로 보냈더니 404 였다. 확인 없이 넣은 게 바로 앞의 사례다. */

// ✅ 사실 — 코드가 왜 이 모양인지
/** 경로를 가르는 축은 모델 세대가 아니라 작업 종류다. 세대는 body 의 `model` 로 말한다. */
```

판단 기준: **이 문장이 대화를 모르는 사람에게도 말이 되는가.**
안 되면 사실만 남기고 사건을 지운다. 그 이력은 **커밋 메시지**가 갖는다.

단, `/** 미검증. */` 처럼 **현재 상태를 알리는 표시**는 사건이 아니므로 남긴다 —
읽는 사람이 무엇이 확인됐고 무엇이 아직 추측인지 알아야 하기 때문이다.

### 모듈 경계를 넘는 참조는 가장 얇게

모듈 안에서는 FK 와 JPA 연관관계를 쓰지만, 경계를 넘을 때는 `Long` 하나만 넘긴다.
`ApplicationModules.verify()` 는 **타입 참조만** 잡고 스키마 결합은 못 잡는다.

서브패키지는 Modulith 에서 internal 이라 다른 모듈이 못 본다.
포트 시그니처에 등장하는 타입이 서브패키지에 있으면 `@NamedInterface` 로 **명시적으로** 노출한다.

```kotlin
@NamedInterface("domain")
enum class AspectRatio(...)
```

base 패키지에 두면 자동으로 공개되지만, 애노테이션을 쓰면 **무엇을 왜 열었는지가 코드에 남는다.**
중첩 클래스(`sealed` 하위 타입 등)에도 각각 붙여야 한다 — 빠뜨리면 그 타입을 처음 쓰는 곳에서 빌드가 깨진다.

### 어댑터는 사실을 답하고, application 이 정책을 정한다

`supports()` 는 Boolean 을 돌려주고 예외를 던지지 않는다.
못 하는 요청을 어떻게 할지(거절·다른 Provider·근사)는 부르는 쪽이 정한다.

찾지 못한 것은 예외가 아니라 `null` 이다 — 호출 지점마다 처리가 갈리기 때문이다.

### 검증 위치가 응답 코드를 정한다

요청 바인딩·값 객체의 `require` 에서 걸리면 400 이고, 서비스에서 던지면 500 이다.
사용자 입력의 불변식은 도메인 타입 생성자에 둔다.

### 저장되는 값의 표기를 고정한다

DB JSON 컬럼과 API 에 나가는 enum 은 `@get:JsonValue` 로 표기를 정한다.
안 붙이면 상수 이름(`ONE_K`)이 그대로 박혀서, 리네임하는 순간 기존 행을 못 읽는다.

### 스키마는 마이그레이션이 정한다

엔티티를 고쳤으면 `src/main/resources/db/migration` 에 마이그레이션을 **같은 커밋에** 넣는다.
`ddl-auto` 는 로컬에서도 `validate` 라, 빠뜨리면 기동이 실패한다 — 배포에서 알게 되는 것보다 낫다.

`ddl-auto: update` 가 못 하는 일이 정확히 사고가 나는 자리다.

| 하는 일 | 안 하는 일 |
|---|---|
| 테이블·컬럼 추가 | **기존 행이 있는 테이블에 NOT NULL 컬럼 추가** (WARN 으로 삼키고 넘어간다) |
| | **체크 제약 갱신** (아예 보지 않는다) |
| | **컬럼·제약 삭제** |

마이그레이션은 PostgreSQL 문법이라 H2 를 쓰는 테스트에서는 돌지 않는다.
`SchemaMigrationTest` 하나만 Testcontainers 로 진짜 PostgreSQL 에 걸어 엔티티와 대조한다.

## 커밋

- `feat:` · `fix:` · `docs:` · `chore:` 접두사 + 한글 제목
- 본문은 **무엇을 했는지가 아니라 왜 그렇게 했는지**. 최대 3~4 항목
- 관심사 단위로 나눈다. 한 커밋이 혼자 설명돼야 한다

## 과금되는 테스트

실제 외부 API 를 부르는 테스트는 **두 겹**으로 막는다.

1. 전용 환경변수로 명시적 의사표시 (`OPENAI_LIVE=1`)
2. 프로젝트 전용 키 이름만 읽는다 — 공용 `OPENAI_API_KEY` 는 다른 도구와 공유되므로 읽지 않는다

키가 설정돼 있는 것은 앱을 띄우기 위한 정상 상태지 돈을 써도 좋다는 뜻이 아니다.
